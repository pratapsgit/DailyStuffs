package main

import (
	"fmt"
)

var x string = `We are here to print a string
	that holds the Jackal. Lion "the King"
	is on the way to reach at this place by
			an hour...............
	from now.`

func main() {
	fmt.Printf("We have the string \"%s\"\n", x)
}
